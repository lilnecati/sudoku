//  SudokuViewModel.swift
//  Sudoku
//
//  Created by Necati Yıldırım on 29.12.2024.
//

import Foundation
import SwiftUI
import Combine
import CoreData
import AudioToolbox
import AVFoundation

// Position yapısı
/* 
struct Position: Hashable {
    let row: Int
    let col: Int
}

class SudokuViewModel: ObservableObject {
    // Sudoku tahtası
    @Published var board: SudokuBoard
    // Seçili hücre
    @Published var selectedCell: (row: Int, column: Int)?
    // Geçersiz hücrelerin listesi
    @Published var invalidCells: Set<Position> = []
    // Oyun süresi
    @Published var elapsedTime: TimeInterval = 0
    // Oyunun durumu
    @Published var gameState: GameState = .playing
    // Kalem modu - not almak için
    @Published var pencilMode: Bool = false
*/

// BU DOSYA ARTIK KULLANILMIYOR
// SudokuViewModel.swift dosyası ana dosya olarak kullanılacaktır
// BACKUP OLARAK SAKLANMAKTADIR
